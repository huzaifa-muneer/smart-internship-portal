import { getDb } from '@/lib/db';import { requireUser } from '@/lib/auth';import { toWorkbook } from '@/lib/excel';
export const dynamic='force-dynamic';
export async function GET(req){await requireUser(req,['admin']);const db=await getDb();const rows=db.prepare('SELECT * FROM internships ORDER BY id DESC').all();const url=new URL(req.url);if(url.searchParams.get('format')==='csv'){const header=Object.keys(rows[0]||{});const csv=[header.join(','),...rows.map(r=>header.map(h=>JSON.stringify(r[h]??'')).join(','))].join('\n');return new Response(csv,{headers:{'Content-Type':'text/csv','Content-Disposition':'attachment; filename="internships.csv"'}})}const buf=toWorkbook(rows);return new Response(buf,{headers:{'Content-Type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','Content-Disposition':'attachment; filename="internships.xlsx"'}})}
